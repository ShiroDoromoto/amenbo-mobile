# 手元で使うツールの版。asdf / mise がこのファイルを読む。
#
# 宣言が無いと「素のシェルでは `make test` が落ちる」になる。ここに書いてあるものは
# `mise install` で揃う。
#
# **flutter は CI（.github/workflows/ci.yml の versions ジョブ）が同じ行を読む。**
# ここが唯一の持ち主なので、上げれば手元と CI が同時に動く。
flutter 3.44.9
